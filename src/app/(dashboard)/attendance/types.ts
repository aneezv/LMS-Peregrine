export type SessionModuleListItem = {
  moduleId: string
  courseId: string
  courseTitle: string
  courseCode: string
  moduleTitle: string
  moduleType: string
  weekIndex: number
  attendanceSubmitted: boolean
}
